<div class="layout layout--col layout--center gap--small">
  <span class="title title--large">{{ temperature }}°C</span>
  <span class="label">{{ condition }} · H {{ high }}° L {{ low }}°</span>
</div>

<div class="title_bar">
  <span class="title">Weather</span>
  <span class="instance">{{ location }}</span>
</div>
