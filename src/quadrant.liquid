<div class="layout layout--col layout--center">
  <span class="title">{{ temperature }}°C</span>
  <span class="label">{{ condition }}</span>
</div>

<div class="title_bar">
  <span class="title">Weather</span>
</div>
